  <%= simple_form_for([@article, @article.reviews.build]) do |f| %>
    <%= f.input :rating %>
    <%= f.input :comment %>
    <%= f.button :submit %>
  <% end %>

<%= simple_form_for([@article1, @article1.reviews.build]) do |f| %>
  <%= f.input :rating %>
  <%= f.input :comment %>
  <%= f.button :submit %>
<% end %>
